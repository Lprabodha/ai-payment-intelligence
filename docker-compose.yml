version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: ai-payment-intelligence-app
    ports:
      - "8010:8010"
    volumes:
      - ./src/models:/src/models
    env_file:
      - .env
